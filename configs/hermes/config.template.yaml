skills:
  external_dirs:
    - /mnt/d/Codex_Project/intellectual_tutor/skills

platforms:
  feishu:
    enabled: true
    extra:
      domain: "feishu"
      connection_mode: "websocket"
      # Fill these locally in $HERMES_HOME/config.yaml or prefer environment variables:
      # app_id: "cli_a1b2c3"
      # app_secret: "secret"

# Keep secrets in $HERMES_HOME/.env, not in this repo.
